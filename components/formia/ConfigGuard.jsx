'use client'

/**
 * ConfigGuard — bypass temporaire
 * Le système de licence sera reconfiguré ultérieurement (SaaS vs self-hosted)
 */
export function ConfigGuard({ children }) {
  return <>{children}</>
}
